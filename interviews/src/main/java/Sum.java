
public class Sum {

    /**
     *
     * @param numbers   The size of the array and the integers will always be greater than 0.
     * @param sum       Will always be greater than 0.
     * @return          The number of ways to sum up to parameter "sum" using the integers within "numbers" - repetition is allowed.
     *                  Ordering does not make a sum unique (e.g. "2+5" is the same as "5+2").
     *                  Example:
     *                              numbers = { 5, 3, 9 }
     *                              sum = 17
     *                              number of ways to sum = 2
     *                              3+3+3+3+5
     *                              3+5+9
     */
    public static int getNumberOfWaysToSumWithRepetition(int[] numbers, int sum) {
        return 0;
    }
}
